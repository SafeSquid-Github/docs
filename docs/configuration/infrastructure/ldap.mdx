---
title: Integrate LDAP
description: Integrate with Microsoft Active Directory or OpenLDAP.  

SafeSquid can also use the Directory Server for authenticating the users.  

This will be useful specially when a user cannot be authenticated via Kerberos/SSO.
sidebar_label: Integrate LDAP
tags: [Infrastructure, configuration]
---

# Integrate LDAP

Integrate with Microsoft Active Directory or OpenLDAP.  

SafeSquid can also use the Directory Server for authenticating the users.  

This will be useful specially when a user cannot be authenticated via Kerberos/SSO.

## Global Settings

Configure section-wide settings that apply to all entries.

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| **Enabled** | `BOOL` | `ldap_enabled` | Turn on or off LDAP integration (connecting to Active Directory or other directory services). |


## Configuration Fields

The following fields are available when creating rules in this section.

### Enabled

**Type:** `BOOL`  
**Default:** `enabled`  



Turn on or off this rule.


**Available Options:**

| Option | Value | Description |
|--------|-------|-------------|
| FALSE | `FALSE` | Turn off this rule. |
| TRUE | `TRUE` | Turn on this rule. |


### Comment

**Type:** `MULTILINE_STRING`  
**Default:** `This entry ...`  



Add notes here to explain what this rule does and why you created it.

This helps you and others understand the purpose of this rule later.


### Host Name

**Type:** `STRING`  




In a network with multiple LDAP Servers, and multiple SafeSquid Proxy Servers deployed in Master-Slave mode, this field can be used to specify the Host Name of the Proxy Server, which will communicate with the LDAP Server configured.

  
Leave this field blank if this is the only SafeSquid proxy, or if you want all the proxies to communicate with the LDAP server configure.


### Ldap FQDN\IP

**Type:** `STRING`  




Enter FQDN of LDAP Server.  

Use IP address of LDAP Server if user can't go for negotiate authentication.

For Kerberos SSO(single sign on) setup specify value like FQDN\\IP (Ex:'ubuntu.safesquid.test\\192.168.XX.XX') and select 'Negotiate' bind type.


### Ldap Port

**Type:** `INT`  
**Default:** `389`  



Specify the Port for LDAP service. Usually this would be 389.


### Use SSL

**Type:** `STRING_SELECT_ONE`  
**Default:** `ssl`  



Communicate with LDAP service using TLS protocol by coping LDAP service CA certificate into security directory and rename CA certificate to LDAP\_CA\_FILE.cer file.

  

Note: If you do not wish to verify server certificate then do not mention CA certificate. SafeSquid will perform SSL with out requesting server certificate.


### Ldap Bind Method

**Type:** `STRING_SELECT_ONE`  
**Default:** `method`  



Choose the method for binding (authenticating) with your Directory Server accoring to appropriate binding mechanisms.


### Query Record Limit

**Type:** `INT`  
**Default:** `1024`  



Specify the maximum number of records that should be fetched in a single LDAP query.


### Ldap User Filter

**Type:** `STRING`  
**Default:** `&amp;(objectclass&#x3D;person)`  



SafeSquid will apply LDAP user filter to narrow the scope of entries requested, to search the users in the LDAP.

  
By default configuartion, LDAP server will return only entries that necessarily match the requirement of objectclass=person and also have atleast one of the attributes of UserPrincipalName or sAMAccountname or uid set.


### Ldap Group Filter

**Type:** `STRING`  
**Default:** `(|(objectclass&#x3D;posixGroup)(objectclass&#x3D;groupofNames)(objectclass&#x3D;group)(objectclass&#x3D;groupOfUniqueNames)(objectclass&#x3D;accessGroup))`  



Specify the LDAP Group Filter. This is required if the LDAP server does not support Reverse Group Membership. Note: Microsoft Windows AD server normally supports Reverse Group Membership. Legacy OpenLDAP implementations may not have support for Reverse Group Membership. If the LDAP server supports Reverse Group Membership then leave this field blank. SafeSquid will apply this filter to narrow the scope of entries requested, to search the groups in the LDAP. For example if: LDAP Group Filter: (|(objectclass=posixGroup)(objectclass=groupofNames)) and, Group Identifier: member,members,memberof SafeSquid will construct filter: (|(objectclass=posixGroup)(objectclass=groupofNames)) and set member, members, memberof as the attributes that may be returned in the ldap search queries. Thus LDAP server will return only entries that necessarily match the requirement of objectclass=posixGroup or objectclass=groupofNames. The DN of the resulting entries will be considered as Groups and values assigned to attributes of member or members or memberof as the users belonging to the respective Groups.


### Ldap Username

**Type:** `STRING`  




Enter the any one Username to bind LDAP Server. Most OpenLDAP servers allow binding anonymosly. In such cases you may leave this blank. For Microsoft AD Servers specify a UserPrincipalName (username@domain.com) or sAMAccountname (Username)


### Ldap Password

**Type:** `STRING`  




Enter the Encrypted Password for the above username to bind to Microsoft AD or LDAP Server. When you click on 'Utilities', you will be presented with a dropdown menu on the left side of the UI. Select 'Encrypt Password' section. For example - Test1 is an user present in Microsoft AD Servers.Enter same password for user Test1, encrypt the password, and copy the encrypted password.


### Ldap Basedn

**Type:** `STRING`  




Enter the Basedn of the LDAP Server in Ldap format. For example, for a domain test1.testdomain1.com, enter dc=testdomain1,dc=com


### Ldap Domain

**Type:** `STRING`  




Specify the LDAP Domain name, for the users that must be authenticated as users of this LDAP server. If you have multiple LDAP servers, SafeSquid will select the appropriate LDAP server to identify and validate a user, by using this as the identifier. For example if you set: LDAP Domain: somedomain.com then all users who present their credentials as username@somedomain.com will be identified and validated from this LDAP server. Note: setting this is extremely important particularly if you are using multiple LDAP servers.


### Login Attributes

**Type:** `STRING_LIST`  
**Default:** `ldap_attributes,ldaploginattr`  



Specify the attributes of a user's entry in LDAP server, that can be permitted for being used for validating user's credentials. Any or all attributes that will be unique for each user in the LDAP server, can be specified here.  
SafeSquid extracts the user's DN using these attributes, and uses the DN, and the user's supplied password to bind with the LDAP server.  
For example if you could set:  
Login Attributes: sAMAccountname,uid,UserPrinicipalName,mail and thus allow your users to use their sAMAccountname or uid or UserPrinicipalName or emailid for the purpose of authentication and profiling. Specifying Attributes that may not be supported by the LDAP server is permissible, though not effective. Note: the list of Login Attributes is comma separated, and unnecessary spaces must be removed.


### Group Identifier

**Type:** `STRING_LIST`  
**Default:** `ldap_group_attributes,ldapgroupid`  



Specify the LDAP Attributes that may be used to as Group Identifiers identify a user's group membership. For example: Group Identifiers: member,memberof,members,memberuid,memberid,uniquemember If Reverse Group Membership has been implemented on the LDAP server the values assigned to these attributes for a user, will be considered as their group memberships. In such cases the entries resulting in the LDAP query for user search, on the basis of Login Attributes all values assigned to the specified Group Identifiers will be considered as groups to which the user subscribes to.  
Typically in case of Microsoft AD, you could have left the text box for Group Filter empty, and values assigned to memberof attributes would have been considered as the user's groups. Legacy implementations of OpenLDAP does not support Reverse Group Membership by default. In such case you would have specified an appropriate value in the Group Filter text box. SafeSquid would then be ale to extract all the entries in LDAP that qualify as Groups. Then the values assigned to the attributes listed in the Group Filter for each of the entries would be considered as users belonging to these Groups. Note: Specifying Attributes that may not be supported by the LDAP server is harmless, though not effective.&  The list of Group Identifier is comma separated, and unnecessary spaces must be removed.



## Subsections

This section contains the following subsections for organizing rules:

### LDAP servers

Define your LDAP server entries here.

Users from all the enabled entries will be fetched by SafeSquid and can be viewed in 'View LDAP cache'.

:::tip Quick Add
This subsection supports quick-add wizard for faster configuration.
:::

### LDAP Entries

View list of LDAP entries that are fetched by SafeSquid from the LDAP service(s) configured.




## Usage Examples

:::info Coming Soon
Practical examples for this section will be added soon. In the meantime, refer to the field descriptions above for guidance.
:::

## Related Sections

- [Getting Started](/docs/getting-started/introduction)
- [Installation](/docs/getting-started/installation)



---

**Section ID:** `ldap`  
**Category:** Infrastructure  
**Wizard Available:** Yes
